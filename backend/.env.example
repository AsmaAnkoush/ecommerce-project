# AWS S3 — copy this file to .env and fill in real values (never commit .env)
AWS_ACCESS_KEY=your-iam-access-key-id
AWS_SECRET_KEY=your-iam-secret-access-key

# Optional — defaults are already set in application.properties
# AWS_REGION=eu-north-1
# AWS_BUCKET_NAME=iwearboutique-images
